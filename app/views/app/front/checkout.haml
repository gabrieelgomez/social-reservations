!!!
%html.no-js{:lang => "en"}
  %head
    = render 'app/layouts/head'

  %body{style:"padding-top:120px;"}
    %header
      = render 'app/layouts/navbar'


    //transfer invoice
    = render 'app/front/invoice/vehicles/checkout_transfers'

    %footer.footer-copyright
      = render 'app/layouts/footer'
    %script{:src => "https://maps.googleapis.com/maps/api/js?v=3.exp&sensor=false&libraries=places"}

    :javascript
      $('.navigation-front').removeClass('background-hide');
      $('.navigation-front').addClass('background-show');
      $(document).ready(function(){
        // $('[data-toggle="datepicker"]').datepicker();
        // $('#destinations-booking_a').selectize({});
        // $('#destinations-booking_b').selectize({});
      });
