= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :role => 'form'}) do |f|
  = devise_error_messages!
  .form-label-group
    = f.input :name, :autofocus => true, class: 'form-control', label: false, :placeholder => "Nombre"
  .form-label-group
    -#= f.label :email
    = f.email_field :email, :autofocus => true, class: 'form-control', label: false, :placeholder => "E-mail"
  .form-label-group
    -#= f.label :password
    = f.password_field :password, class: 'form-control', label: false, :placeholder => "Password"
  .form-label-group
    -#= f.label :password_confirmation
    = f.password_field :password_confirmation, class: 'form-control', label: false, :placeholder => "Password confirmation"
  .box-login-btn
    = f.submit 'Crear Cuenta', :class => 'btn btn-lg btn-primary btn-block btn-login'
