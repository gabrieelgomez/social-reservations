#login-front.login-front{style: "background-image: url(/assets/img/wallpaper-receptivo.jpg)"}
  .mask-front
    .form-signin
      .text-center.mb-4
        %img.mb-4{:alt => "", :src => "/assets/img/login/logo.svg", :width => "150px"}/
      %ul.nav.nav-tabs
        %li.active
          -#%a{"data-toggle" => "tab", :href => "#1"} Sign in
          = link_to 'Iniciar Sesión','#1', data: { toggle: 'tab'}
        %li
          -#%a{"data-toggle" => "tab", :href => "#2"} Register
          = link_to 'Crear Registro','#2', data: { toggle: 'tab'}
        %li
          -#%a{"data-toggle" => "tab", :href => "#3"} Recovery
          = link_to 'Recuperar Contraseña','#3', data: { toggle: 'tab'}
      .tab-content
        #1.tab-pane.active

          -#%input.micheckbox{:name => "vehicle", :type => "checkbox", :value => "Bike"}/


          .box__agency
            .container
              .row
                .col-xs-12.pull-right
                  .box-agent
                    %span.agent ¿Eres Agencia?
                    %label.switch
                      %input.js-input-agency{:type => "checkbox"}/
                      %span.slider.round
          = render 'devise/sessions/sign_in'#, resource: resource, resource_name: resource_name
        #2.tab-pane
          = render 'devise/registrations/sign_up'
        #3.tab-pane
          = render 'devise/passwords/new'
        .copyright-login
          .go-back-home
            = link_to root_path do
              %span
                %i.icon-home.icons
              Ir a la página principal
          %p.text-center Receptivo Colombia © | 2018




:scss


  [type="checkbox"] {
    display: block !important;
    transition        : all .3s ease;
    -webkit-transition: all .3s ease;
  }
  .switch {
    position: relative;
    display: inline-block;
    width: 49px;
    height: 23px;
    vertical-align: middle;
    margin-left: 10px;
  }

  .switch input { 
    opacity: 0;
    width: 0;
    height: 0;
  }

  .slider {
    position: absolute;
    cursor: pointer;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: #ccc;
    -webkit-transition: .4s;
    transition: .4s;
  }

  .slider:before {
    position: absolute;
    content: "";
    height: 26px;
    width: 26px;
    left: 4px;
    bottom: 4px;
    background-color: white;
    -webkit-transition: .4s;
    transition: .4s;
  }

  input:checked + .slider {
    background-color: #43b7e8;
  }

  input:focus + .slider {
    box-shadow: 0 0 1px #43b7e8;
  }

  input:checked + .slider:before {
    -webkit-transform: translateX(26px);
    -ms-transform: translateX(26px);
    transform: translateX(26px);
  }

  /* Rounded sliders */
  .slider.round {
    border-radius: 34px;
  }

  .slider.round:before {
    border-radius: 50%;
  }

  .agent{
    color: #43b7e8;
    font-weight: 500;
    font-family: 'Poppins', sans-serif;
    border: 0;
    background-color: white;
    font-weight: bold;
    font-size: 12px;
    text-align: center;
  }

  .box-agent{
      display: flex;
    justify-content: flex-end;
    align-items: center;
    padding-right: 19px;
    padding-left: 19px;
    margin-top: -9px;
    margin-bottom: 9px;
  }
  .show-input{
    display: block;
  }
  .hide-input{
    display: none;
  }


:javascript
  //$( '.js-input-agency' ).on( 'click', function() {
  $(".js-input-agency").on( 'change', function() {
      if( $(this).is(':checked') ){
          $('.input-agency').removeClass('hide-input');
          $('.input-agency').addClass('show-input');
      } else {
          $('.input-agency').removeClass('show-input');
          $('.input-agency').addClass('hide-input');
      }
  });